# 베리어프리 영화 서비스 - 최종 완전판 (설문조사 제외)

## 📋 시스템 개요

베리어프리(Barrier-Free) 영화 서비스를 위한 완전한 데이터베이스 설계입니다.
시각/청각 장애인을 위한 화면해설(Audio Commentary)과 한글자막(Closed Caption) 서비스를 제공합니다.

---

## 🎯 핵심 기능

### 1. 영화 관리
- 베리어프리 영화 메타데이터 관리
- 화면해설/한글자막 파일 경로 저장
- 장르별, 제작사별 분류
- 최신작/인기작 특별 목록 관리

### 2. 회원 관리
- 일반 회원가입 + 소셜 로그인 (카카오/네이버/구글/애플)
- 찜하기 기능
- 다운로드 이력 관리
- 장애 유형별 맞춤 서비스

### 3. 마일리지 시스템 (간소화)
- 기본 포인트 적립/사용
- 회원가입, 다운로드, 리뷰 작성 시 포인트 적립
- 광고 제거, 영화 관람권, 기부 등 사용

### 4. 공지사항
- 서비스 업데이트, 이벤트 공지
- 중요 공지 상단 고정
- 조회수 통계
- 푸시 알림 연동

### 5. 푸시 알림
- 공지사항 알림
- 신규 영화 추가 알림
- 포인트 적립 알림
- 이벤트 알림

---

## 📊 전체 테이블 구성 (12개)

```
[영화 관련 - 5개]
1. GENRE (장르)
2. PRODUCTION_COMPANY (제작사)
3. MOVIE (영화 메타데이터)
4. FEATURED_LIST (특별 목록)
5. FEATURED_LIST_MOVIE (특별목록-영화 연결)

[회원 관련 - 3개]
6. USER (회원)
7. USER_FAVORITE (찜하기)
8. USER_DOWNLOAD (다운로드 이력)

[마일리지 - 2개]
9. USER_POINT (포인트 계정)
10. POINT_HISTORY (포인트 이력)

[커뮤니케이션 - 2개]
11. NOTICE (공지사항)
12. PUSH_NOTIFICATION (푸시 알림)
```

---

## 🗂️ 테이블 상세 설계

### 1. GENRE (장르 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| genre_id | INT | 장르 고유 식별자 | PK, AUTO_INCREMENT |
| genre_name | VARCHAR(50) | 장르명 | NN, UNIQUE |
| genre_description | TEXT | 장르 설명 | NULL |
| created_at | TIMESTAMP | 생성일시 | NN, DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 수정일시 | NN, DEFAULT CURRENT_TIMESTAMP ON UPDATE |

---

### 2. PRODUCTION_COMPANY (제작사 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| company_id | INT | 제작사 고유 식별자 | PK, AUTO_INCREMENT |
| company_name | VARCHAR(200) | 제작사명 | NN |
| country | VARCHAR(100) | 국가 | NULL |
| established_year | INT | 설립연도 | NULL |
| company_website | VARCHAR(255) | 웹사이트 | NULL |
| created_at | TIMESTAMP | 생성일시 | NN, DEFAULT CURRENT_TIMESTAMP |

---

### 3. MOVIE (영화 메타데이터 테이블) ⭐ 중심

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| movie_id | INT | 영화 고유 식별자 | PK, AUTO_INCREMENT |
| title | VARCHAR(255) | 영화 제목 | NN |
| original_title | VARCHAR(255) | 원제 | NULL |
| release_date | DATE | 개봉일 | NULL |
| director_name | VARCHAR(200) | 감독명 | NULL |
| running_time | INT | 러닝타임(분) | NULL |
| synopsis | TEXT | 줄거리 | NULL |
| poster_url | VARCHAR(500) | 포스터 이미지 URL | NULL |
| rating | VARCHAR(20) | 관람등급 | NULL |
| **has_audio_commentary** | TINYINT(1) | 화면해설 제공 여부 | NN, DEFAULT 0 |
| **has_closed_caption** | TINYINT(1) | 한글자막 제공 여부 | NN, DEFAULT 0 |
| **audio_commentary_file** | VARCHAR(500) | 화면해설 파일 경로 | NULL |
| **closed_caption_file** | VARCHAR(500) | 한글자막 파일 경로 | NULL |
| is_latest | TINYINT(1) | 최신작 표시 | NN, DEFAULT 0 |
| is_popular | TINYINT(1) | 인기작 표시 | NN, DEFAULT 0 |
| genre_id | INT | 장르 FK | FK(GENRE) |
| company_id | INT | 제작사 FK | FK(PRODUCTION_COMPANY) |
| created_at | TIMESTAMP | 생성일시 | NN, DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 수정일시 | NN, DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**인덱스:**
- `idx_barrier_free (has_audio_commentary, has_closed_caption)`
- `idx_title (title)`
- `idx_release_date (release_date)`

---

### 4. FEATURED_LIST (특별 목록 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| list_id | INT | 목록 고유 식별자 | PK, AUTO_INCREMENT |
| list_name | VARCHAR(100) | 목록명 | NN, UNIQUE |
| list_description | TEXT | 목록 설명 | NULL |
| max_items | INT | 최대 항목 수 | NULL |
| is_active | TINYINT(1) | 활성화 여부 | NN, DEFAULT 1 |
| display_order | INT | 표시 순서 | NULL |
| created_at | TIMESTAMP | 생성일시 | NN, DEFAULT CURRENT_TIMESTAMP |

**예시:**
- 최신작 (최대 5편)
- 인기작 (최대 10편)
- 이번 주 추천작

---

### 5. FEATURED_LIST_MOVIE (특별목록-영화 연결 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| featured_id | INT | 연결 고유 식별자 | PK, AUTO_INCREMENT |
| list_id | INT | 목록 FK | FK(FEATURED_LIST), NN |
| movie_id | INT | 영화 FK | FK(MOVIE), NN |
| display_order | INT | 목록 내 순서 | NN |
| featured_date | DATE | 등록일 | NN, DEFAULT CURRENT_DATE |
| created_at | TIMESTAMP | 생성일시 | NN, DEFAULT CURRENT_TIMESTAMP |

**제약조건:**
- `UNIQUE (list_id, movie_id)` - 한 목록에 같은 영화 중복 등록 방지

---

### 6. USER (회원 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| user_id | BIGINT | 회원 고유 식별자 | PK, AUTO_INCREMENT |
| email | VARCHAR(255) | 이메일 | UNIQUE |
| password_hash | VARCHAR(255) | 비밀번호 해시 (bcrypt) | NULL |
| username | VARCHAR(100) | 사용자명 | NN |
| phone | VARCHAR(20) | 휴대폰 번호 | NULL |
| profile_image | VARCHAR(500) | 프로필 이미지 URL | NULL |
| **auth_provider** | VARCHAR(20) | 인증 제공자 | NN |
| **social_id** | VARCHAR(255) | 소셜 로그인 고유 ID | NULL |
| **disability_type** | VARCHAR(50) | 장애 유형 | NULL |
| is_active | TINYINT(1) | 활성화 여부 | NN, DEFAULT 1 |
| last_login_at | TIMESTAMP | 마지막 로그인 | NULL |
| created_at | TIMESTAMP | 가입일시 | NN, DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 수정일시 | NN, DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**auth_provider 값:**
- `email`: 일반 이메일 회원가입
- `kakao`: 카카오 로그인
- `naver`: 네이버 로그인
- `google`: 구글 로그인
- `apple`: 애플 로그인

**disability_type 값:**
- `visual`: 시각장애
- `hearing`: 청각장애
- `none`: 해당 없음
- `NULL`: 미선택

**인덱스:**
- `idx_email (email)`
- `idx_social (auth_provider, social_id)`

---

### 7. USER_FAVORITE (찜하기 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| favorite_id | BIGINT | 찜 고유 식별자 | PK, AUTO_INCREMENT |
| user_id | BIGINT | 회원 FK | FK(USER), NN |
| movie_id | INT | 영화 FK | FK(MOVIE), NN |
| created_at | TIMESTAMP | 찜한 일시 | NN, DEFAULT CURRENT_TIMESTAMP |

**제약조건:**
- `UNIQUE (user_id, movie_id)` - 중복 찜 방지
- `FK: user_id → USER ON DELETE CASCADE`
- `FK: movie_id → MOVIE ON DELETE CASCADE`

---

### 8. USER_DOWNLOAD (다운로드 이력 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| download_id | BIGINT | 다운로드 고유 식별자 | PK, AUTO_INCREMENT |
| user_id | BIGINT | 회원 FK | FK(USER), NN |
| movie_id | INT | 영화 FK | FK(MOVIE), NN |
| file_type | VARCHAR(20) | 파일 유형 | NN |
| file_path | VARCHAR(500) | 다운로드 파일 경로 | NN |
| download_at | TIMESTAMP | 다운로드 일시 | NN, DEFAULT CURRENT_TIMESTAMP |

**file_type 값:**
- `audio_commentary`: 화면해설
- `closed_caption`: 한글자막

**제약조건:**
- `CHECK: file_type IN ('audio_commentary', 'closed_caption')`
- `FK: user_id → USER ON DELETE CASCADE`
- `FK: movie_id → MOVIE ON DELETE SET NULL`

---

### 9. USER_POINT (포인트 계정 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| point_id | BIGINT | 포인트 고유 식별자 | PK, AUTO_INCREMENT |
| user_id | BIGINT | 회원 FK | FK(USER), NN, UNIQUE |
| total_points | INT | 현재 보유 포인트 | NN, DEFAULT 0 |
| earned_points | INT | 누적 적립 포인트 | NN, DEFAULT 0 |
| used_points | INT | 누적 사용 포인트 | NN, DEFAULT 0 |
| created_at | TIMESTAMP | 생성일시 | NN, DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 수정일시 | NN, DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**계산 공식:**
```
total_points = earned_points - used_points
```

**제약조건:**
- `CHECK: total_points >= 0`
- `UNIQUE (user_id)` - 회원당 1개 계정

---

### 10. POINT_HISTORY (포인트 이력 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| history_id | BIGINT | 이력 고유 식별자 | PK, AUTO_INCREMENT |
| user_id | BIGINT | 회원 FK | FK(USER), NN |
| point_type | VARCHAR(10) | 포인트 유형 | NN |
| points | INT | 포인트 변동량 | NN |
| reason | VARCHAR(100) | 사유 | NN |
| balance_after | INT | 거래 후 잔액 | NN |
| created_at | TIMESTAMP | 거래 일시 | NN, DEFAULT CURRENT_TIMESTAMP |

**point_type 값:**
- `earn`: 적립
- `use`: 사용

**reason 예시:**
- 회원가입 축하
- 베리어프리 다운로드
- 영화 리뷰 작성
- 매일 출석 체크
- 광고 제거 구매
- 영화 관람권 교환

**인덱스:**
- `idx_user_created (user_id, created_at)`

---

### 11. NOTICE (공지사항 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| notice_id | BIGINT | 공지 고유 식별자 | PK, AUTO_INCREMENT |
| title | VARCHAR(200) | 공지 제목 | NN |
| content | TEXT | 공지 내용 | NN |
| notice_type | VARCHAR(20) | 공지 유형 | NN |
| is_important | TINYINT(1) | 중요 공지 여부 (상단 고정) | NN, DEFAULT 0 |
| is_push_sent | TINYINT(1) | 푸시 발송 여부 | NN, DEFAULT 0 |
| push_title | VARCHAR(100) | 푸시 제목 | NULL |
| push_message | VARCHAR(200) | 푸시 메시지 | NULL |
| thumbnail_url | VARCHAR(500) | 썸네일 이미지 URL | NULL |
| link_url | VARCHAR(500) | 클릭 시 이동 URL | NULL |
| view_count | INT | 조회수 | NN, DEFAULT 0 |
| published_at | TIMESTAMP | 게시 일시 | NULL |
| expires_at | TIMESTAMP | 만료 일시 | NULL |
| created_by | BIGINT | 작성자 (관리자 ID) | NULL |
| created_at | TIMESTAMP | 작성 일시 | NN, DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 수정 일시 | NN, DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**notice_type 값:**
- `general`: 일반 공지
- `update`: 서비스 업데이트
- `event`: 이벤트/프로모션
- `important`: 긴급/중요 공지

**인덱스:**
- `idx_important (is_important, published_at)`
- `idx_published (published_at)`
- `idx_type (notice_type)`

---

### 12. PUSH_NOTIFICATION (푸시 알림 테이블)

| 필드명 | 데이터 타입 | 설명 | 제약조건 |
|--------|------------|------|----------|
| push_id | BIGINT | 푸시 고유 식별자 | PK, AUTO_INCREMENT |
| push_type | VARCHAR(20) | 푸시 유형 | NN |
| title | VARCHAR(100) | 푸시 제목 | NN |
| message | VARCHAR(200) | 푸시 메시지 | NN |
| image_url | VARCHAR(500) | 푸시 이미지 URL | NULL |
| link_type | VARCHAR(20) | 링크 타입 | NULL |
| link_id | BIGINT | 링크 대상 ID | NULL |
| link_url | VARCHAR(500) | 외부 링크 URL | NULL |
| target_users | VARCHAR(50) | 대상 사용자 | NN, DEFAULT 'all' |
| target_user_ids | TEXT | 특정 사용자 ID 목록 (JSON) | NULL |
| scheduled_at | TIMESTAMP | 발송 예약 시간 | NULL |
| sent_at | TIMESTAMP | 실제 발송 시간 | NULL |
| sent_count | INT | 발송 성공 수 | NN, DEFAULT 0 |
| read_count | INT | 읽음 수 | NN, DEFAULT 0 |
| click_count | INT | 클릭 수 | NN, DEFAULT 0 |
| created_by | BIGINT | 작성자 (관리자 ID) | NULL |
| created_at | TIMESTAMP | 작성 일시 | NN, DEFAULT CURRENT_TIMESTAMP |

**push_type 값:**
- `notice`: 공지사항
- `movie`: 신규 영화
- `point`: 포인트 적립
- `event`: 이벤트

**link_type 값:**
- `notice`: 공지사항 상세
- `movie`: 영화 상세
- `url`: 외부 링크

**target_users 값:**
- `all`: 전체 사용자
- `visual`: 시각장애인만
- `hearing`: 청각장애인만
- `specific`: 특정 사용자 (target_user_ids 사용)

**인덱스:**
- `idx_push_type (push_type)`
- `idx_scheduled (scheduled_at)`
- `idx_sent (sent_at)`

---

## 🔗 ERD 관계도

```
         USER (회원)
           │
           ├─── (1:1) ──── USER_POINT (포인트 계정)
           │                   │
           ├─── (1:N) ──< POINT_HISTORY (포인트 이력)
           │
           ├─── (1:N) ──< USER_FAVORITE (찜하기) ──> MOVIE
           │                                            │
           ├─── (1:N) ──< USER_DOWNLOAD (다운로드) ────┘
           │                                            │
           │                                            │
    GENRE (1) ─────────────< MOVIE                     │
                                │                       │
    PRODUCTION_COMPANY (1) ─────┘                      │
                                                        │
    FEATURED_LIST (1) ──< FEATURED_LIST_MOVIE >── (N) ─┘


    NOTICE (공지사항) - 독립 테이블
    
    PUSH_NOTIFICATION (푸시 알림) - 독립 테이블
```

---

## 💰 마일리지 정책

### 적립 정책

| 활동 | 포인트 | 제한 |
|------|--------|------|
| 회원가입 | 1,000P | 1회 |
| 베리어프리 다운로드 | 50P | 1일 5회 |
| 영화 리뷰 작성 | 100P | 1일 3회 |
| 매일 출석 체크 | 10P | 1일 1회 |

### 사용 방안

| 항목 | 포인트 |
|------|--------|
| 광고 제거 (1개월) | 500P |
| CGV 영화 관람권 | 8,000P |
| 메가박스 영화 관람권 | 8,000P |
| 시각장애인협회 기부 | 1,000P~ |
| 청각장애인협회 기부 | 1,000P~ |

---

## 💻 주요 쿼리 예시

### 1. 영화 관련

#### 베리어프리 영화 검색
```sql
SELECT movie_id, title, poster_url, 
       has_audio_commentary, has_closed_caption
FROM MOVIE
WHERE has_audio_commentary = 1 OR has_closed_caption = 1
ORDER BY release_date DESC
LIMIT 20;
```

#### 화면해설 제공 영화만 검색
```sql
SELECT * FROM MOVIE 
WHERE has_audio_commentary = 1
ORDER BY release_date DESC;
```

#### 최신작 목록 조회
```sql
SELECT m.* FROM MOVIE m
INNER JOIN FEATURED_LIST_MOVIE flm ON m.movie_id = flm.movie_id
INNER JOIN FEATURED_LIST fl ON flm.list_id = fl.list_id
WHERE fl.list_name = '최신작'
ORDER BY flm.display_order;
```

---

### 2. 회원 관련

#### 이메일 로그인
```sql
SELECT user_id, email, password_hash, username, disability_type
FROM USER 
WHERE email = 'user@example.com' 
  AND auth_provider = 'email'
  AND is_active = 1;
```

#### 소셜 로그인
```sql
SELECT user_id, email, username, disability_type
FROM USER 
WHERE auth_provider = 'kakao' 
  AND social_id = '12345678'
  AND is_active = 1;
```

---

### 3. 찜하기

#### 찜 추가
```sql
INSERT INTO USER_FAVORITE (user_id, movie_id) 
VALUES (1, 100);
```

#### 찜 취소
```sql
DELETE FROM USER_FAVORITE 
WHERE user_id = 1 AND movie_id = 100;
```

#### 내가 찜한 영화 목록
```sql
SELECT m.movie_id, m.title, m.poster_url,
       m.has_audio_commentary, m.has_closed_caption,
       uf.created_at AS favorited_at
FROM USER_FAVORITE uf
INNER JOIN MOVIE m ON uf.movie_id = m.movie_id
WHERE uf.user_id = 1
ORDER BY uf.created_at DESC;
```

#### 찜 여부 확인
```sql
SELECT EXISTS(
    SELECT 1 FROM USER_FAVORITE 
    WHERE user_id = 1 AND movie_id = 100
) AS is_favorited;
```

---

### 4. 다운로드

#### 다운로드 이력 저장
```sql
INSERT INTO USER_DOWNLOAD (user_id, movie_id, file_type, file_path) 
VALUES (1, 100, 'audio_commentary', '/storage/ac/100_ac.mp3');
```

#### 내 다운로드 이력
```sql
SELECT m.title, ud.file_type, ud.download_at
FROM USER_DOWNLOAD ud
INNER JOIN MOVIE m ON ud.movie_id = m.movie_id
WHERE ud.user_id = 1
ORDER BY ud.download_at DESC
LIMIT 50;
```

#### 최근 다운로드한 영화 (중복 제거)
```sql
SELECT DISTINCT m.movie_id, m.title, m.poster_url,
       MAX(ud.download_at) AS last_download_at
FROM USER_DOWNLOAD ud
INNER JOIN MOVIE m ON ud.movie_id = m.movie_id
WHERE ud.user_id = 1
GROUP BY m.movie_id, m.title, m.poster_url
ORDER BY last_download_at DESC
LIMIT 20;
```

---

### 5. 포인트

#### 포인트 적립
```sql
START TRANSACTION;

-- 포인트 이력 추가
INSERT INTO POINT_HISTORY (user_id, point_type, points, reason, balance_after)
VALUES (1, 'earn', 100, '베리어프리 다운로드', 1100);

-- 포인트 계정 업데이트
UPDATE USER_POINT 
SET total_points = total_points + 100,
    earned_points = earned_points + 100
WHERE user_id = 1;

COMMIT;
```

#### 포인트 사용
```sql
START TRANSACTION;

-- 포인트 이력 추가
INSERT INTO POINT_HISTORY (user_id, point_type, points, reason, balance_after)
VALUES (1, 'use', 500, '광고 제거 구매', 600);

-- 포인트 계정 업데이트
UPDATE USER_POINT 
SET total_points = total_points - 500,
    used_points = used_points + 500
WHERE user_id = 1;

COMMIT;
```

#### 내 포인트 조회
```sql
SELECT total_points, earned_points, used_points
FROM USER_POINT
WHERE user_id = 1;
```

#### 포인트 이력 조회
```sql
SELECT history_id, point_type, points, reason, 
       balance_after, created_at
FROM POINT_HISTORY
WHERE user_id = 1
ORDER BY created_at DESC
LIMIT 20;
```

#### 포인트 랭킹 TOP 10
```sql
SELECT u.username, up.total_points
FROM USER_POINT up
INNER JOIN USER u ON up.user_id = u.user_id
WHERE u.is_active = 1
ORDER BY up.total_points DESC
LIMIT 10;
```

---

### 6. 공지사항

#### 공지사항 목록 (중요 공지 우선)
```sql
SELECT notice_id, title, notice_type, 
       is_important, view_count, published_at
FROM NOTICE
WHERE published_at <= NOW()
  AND (expires_at IS NULL OR expires_at > NOW())
ORDER BY is_important DESC, published_at DESC
LIMIT 20;
```

#### 공지 상세 조회 + 조회수 증가
```sql
-- 조회
SELECT * FROM NOTICE WHERE notice_id = 1;

-- 조회수 증가
UPDATE NOTICE 
SET view_count = view_count + 1 
WHERE notice_id = 1;
```

#### 중요 공지만 조회 (배너용)
```sql
SELECT notice_id, title, notice_type
FROM NOTICE
WHERE is_important = 1
  AND published_at <= NOW()
  AND (expires_at IS NULL OR expires_at > NOW())
ORDER BY published_at DESC
LIMIT 3;
```

---

### 7. 푸시 알림

#### 전체 사용자에게 푸시 발송
```sql
INSERT INTO PUSH_NOTIFICATION (
    push_type, title, message, 
    link_type, link_id, target_users, 
    scheduled_at, created_by
) VALUES (
    'notice', 
    '새로운 공지사항', 
    '베리어프리 신작 영화가 추가되었습니다!',
    'notice', 
    123,
    'all',
    NOW(),
    1
);
```

#### 시각장애인에게만 푸시 발송
```sql
INSERT INTO PUSH_NOTIFICATION (
    push_type, title, message, 
    target_users, scheduled_at
) VALUES (
    'movie', 
    '화면해설 신작 추가', 
    '범죄도시 4 화면해설이 추가되었습니다',
    'visual',
    NOW()
);
```

#### 발송 대기 중인 푸시 조회
```sql
SELECT * FROM PUSH_NOTIFICATION
WHERE sent_at IS NULL
  AND scheduled_at <= NOW()
ORDER BY scheduled_at;
```

#### 푸시 발송 완료 처리
```sql
UPDATE PUSH_NOTIFICATION 
SET sent_at = NOW(), 
    sent_count = 1500
WHERE push_id = 1;
```

---

## 📱 UI/UX 제안

### 홈 화면
```
┌─────────────────────────────────┐
│  🎬 베리어프리 영화              │
├─────────────────────────────────┤
│  🔔 알림 (3)   💎 2,850P       │
│                                 │
│  [최신작]                       │
│  ┌───────────────────────────┐ │
│  │ 🎥 범죄도시 4             │ │
│  │ ✅ 화면해설 · 한글자막    │ │
│  │ 허명행 감독 | 109분       │ │
│  └───────────────────────────┘ │
│                                 │
│  📢 공지사항                    │
│  🔴 베리어프리 신작 50편 추가   │
│     2025.01.22                  │
│                                 │
└─────────────────────────────────┘
```

### 공지사항 화면
```
┌─────────────────────────────────┐
│  📢 공지사항                     │
├─────────────────────────────────┤
│                                 │
│  🔴 [중요] 베리어프리 신작 추가 │
│     2025.01.22  조회 1,234      │
│                                 │
│  📢 서비스 업데이트 안내        │
│     2025.01.20  조회 856        │
│                                 │
│  🎬 1월 추천 영화 리스트        │
│     2025.01.15  조회 2,103      │
│                                 │
│  🎉 이벤트 당첨자 발표          │
│     2025.01.10  조회 567        │
│                                 │
└─────────────────────────────────┘
```

### 마이페이지
```
┌─────────────────────────────────┐
│  내 정보                         │
│  홍길동 (user@example.com)      │
│  [프로필 수정]                   │
├─────────────────────────────────┤
│                                 │
│  💎 내 포인트                   │
│  2,850P                         │
│  [포인트 내역] [사용하기]       │
│                                 │
│  💙 찜한 영화 (23개)            │
│  └→ [전체 보기]                 │
│                                 │
│  📥 다운로드 이력 (156개)       │
│  └→ [전체 보기]                 │
│                                 │
│  ⚙️ 설정                        │
│  [로그아웃] [회원탈퇴]          │
│                                 │
└─────────────────────────────────┘
```

### 푸시 알림 예시
```
┌─────────────────────────────────┐
│  🔔 베리어프리 영화              │
│                                 │
│  새로운 베리어프리 영화가       │
│  추가되었습니다!                │
│                                 │
│  [범죄도시 4] 화면해설·한글자막 │
│  지금 바로 확인하세요           │
│                                 │
│  방금 전                        │
└─────────────────────────────────┘
```

---

## 📊 API 엔드포인트 제안

### 영화
```
GET    /api/movies                     영화 목록
GET    /api/movies/{id}                영화 상세
GET    /api/movies/barrier-free        베리어프리 영화 목록
GET    /api/movies/featured/{list}     특별 목록 영화
```

### 회원
```
POST   /api/auth/register              일반 회원가입
POST   /api/auth/login                 이메일 로그인
POST   /api/auth/social/{provider}     소셜 로그인
GET    /api/users/me                   내 정보
PUT    /api/users/me                   내 정보 수정
DELETE /api/users/me                   회원탈퇴
```

### 찜하기
```
POST   /api/favorites                  찜 추가
DELETE /api/favorites/{movie_id}       찜 취소
GET    /api/favorites                  찜 목록
GET    /api/favorites/check/{movie_id} 찜 여부
```

### 다운로드
```
POST   /api/downloads                  다운로드 이력 저장
GET    /api/downloads                  다운로드 이력
GET    /api/downloads/movies           다운로드한 영화
```

### 포인트
```
GET    /api/points/me                  내 포인트
GET    /api/points/history             포인트 이력
POST   /api/points/earn                포인트 적립
POST   /api/points/use                 포인트 사용
GET    /api/points/ranking             포인트 랭킹
```

### 공지사항
```
GET    /api/notices                    공지 목록
GET    /api/notices/{id}               공지 상세
GET    /api/notices/important          중요 공지
```

### 푸시 알림
```
GET    /api/notifications              알림 목록
GET    /api/notifications/unread       읽지 않은 알림
PUT    /api/notifications/{id}/read    알림 읽음 처리
```

---

## 🎯 개발 체크리스트

### 데이터베이스
- [ ] 12개 테이블 생성
- [ ] 외래 키 제약조건 설정
- [ ] 인덱스 생성
- [ ] 샘플 데이터 입력

### 백엔드
- [ ] 회원가입/로그인 API
- [ ] 소셜 로그인 연동 (카카오/네이버/구글/애플)
- [ ] 영화 CRUD API
- [ ] 찜하기 API
- [ ] 다운로드 이력 API
- [ ] 포인트 적립/사용 API
- [ ] 공지사항 API
- [ ] 푸시 알림 API

### 프론트엔드
- [ ] 회원가입/로그인 화면
- [ ] 영화 목록/상세 화면
- [ ] 찜하기 기능
- [ ] 다운로드 기능
- [ ] 마이페이지
- [ ] 포인트 관리 화면
- [ ] 공지사항 화면
- [ ] 푸시 알림 수신

### 배포
- [ ] DB 마이그레이션
- [ ] 파일 스토리지 설정
- [ ] 푸시 알림 서비스 연동
- [ ] 모니터링 설정

---

이제 **실용적이고 완전한** 베리어프리 영화 서비스 시스템이 완성되었습니다! 🎉
