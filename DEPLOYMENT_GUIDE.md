# AudioView 배포 가이드

## 🎯 화면을 볼 수 있는 5가지 방법

### 1. 로컬 개발 환경 (Flutter 설치 필요)

#### Windows에서 Flutter 설치
```powershell
# 1. Flutter SDK 다운로드
# https://docs.flutter.dev/get-started/install/windows

# 2. 압축 해제 (예: C:\src\flutter)

# 3. 환경 변수 설정
# 시스템 속성 → 환경 변수 → Path에 추가
# C:\src\flutter\bin

# 4. 설치 확인
flutter doctor

# 5. Android 라이선스 동의
flutter doctor --android-licenses
```

#### 앱 실행
```bash
cd C:\Users\정해석\Downloads\audioview

# 의존성 설치
flutter pub get

# 연결된 기기 확인
flutter devices

# 앱 실행
flutter run
```

---

### 2. 온라인 IDE 사용 (설치 불필요) ⭐ 추천

#### FlutLab.io
1. https://flutlab.io 접속
2. "Create New Project" 클릭
3. 프로젝트 파일 업로드
   - `lib/` 폴더 전체
   - `pubspec.yaml`
4. "Run" 버튼 클릭
5. 브라우저에서 앱 미리보기

#### Zapp.run
1. https://zapp.run 접속
2. GitHub 저장소 연결 또는 파일 업로드
3. 자동으로 앱 빌드 및 실행
4. 브라우저 + 모바일에서 테스트 가능

---

### 3. APK 파일 만들어서 배포

#### APK 빌드 (Flutter 설치 후)
```bash
cd C:\Users\정해석\Downloads\audioview

# Release APK 생성
flutter build apk --release

# Split APK (용량 작음)
flutter build apk --split-per-abi --release
```

#### 빌드된 파일 위치
```
C:\Users\정해석\Downloads\audioview\build\app\outputs\flutter-apk\

📦 파일:
- app-release.apk (범용, 약 40-50MB)
- app-armeabi-v7a-release.apk (32bit, 약 20MB)
- app-arm64-v8a-release.apk (64bit, 약 20MB)
- app-x86_64-release.apk (에뮬레이터용)
```

#### APK 설치 방법
1. APK 파일을 안드로이드 폰으로 전송
   - USB 케이블
   - 이메일 첨부
   - Google Drive/Dropbox 공유
   - KakaoTalk 파일 전송

2. 폰에서 APK 파일 열기
   - "출처를 알 수 없는 앱" 허용
   - 설치 진행

3. 앱 실행

---

### 4. 웹 버전 배포 (브라우저에서 실행)

#### 웹 빌드
```bash
# 웹 지원 활성화
flutter config --enable-web

# 웹 빌드
flutter build web --release

# 빌드 결과
# C:\Users\정해석\Downloads\audioview\build\web\
```

#### 웹 호스팅 옵션

**A. Firebase Hosting (무료)**
```bash
# Firebase CLI 설치
npm install -g firebase-tools

# 로그인
firebase login

# 프로젝트 초기화
firebase init hosting

# 배포
firebase deploy
```

**B. Netlify (무료)**
1. https://netlify.com 가입
2. "Add new site" → "Deploy manually"
3. `build/web` 폴더 드래그앤드롭
4. 배포 완료 → URL 생성

**C. Vercel (무료)**
1. https://vercel.com 가입
2. "New Project" → `build/web` 폴더 업로드
3. 자동 배포

**D. GitHub Pages (무료)**
```bash
# build/web 내용을 GitHub 저장소에 푸시
git init
git add .
git commit -m "Deploy AudioView"
git branch -M gh-pages
git push -u origin gh-pages

# GitHub 저장소 Settings → Pages에서 활성화
# URL: https://<username>.github.io/audioview
```

---

### 5. 앱 스토어 배포 (공식 출시)

#### Google Play Store
```bash
# 1. App Bundle 빌드
flutter build appbundle --release

# 2. Play Console에 업로드
# https://play.google.com/console

# 3. 필요한 정보
- 앱 이름: AudioView
- 패키지명: com.audioview.app
- 스크린샷 (여러 화면 크기)
- 앱 아이콘
- 개인정보처리방침 URL
```

**출시 과정**
1. Google Play Console 계정 생성 (₩25,000 일회성 비용)
2. 앱 등록
3. 내부 테스트 → 비공개 테스트 → 공개 출시
4. 검토 (1-3일 소요)

---

## 🎨 각 방법 비교

| 방법 | 속도 | 비용 | 난이도 | 용도 |
|-----|------|------|--------|------|
| 로컬 실행 | ⚡ 즉시 | 무료 | 중 | 개발/테스트 |
| 온라인 IDE | ⚡⚡ 5분 | 무료 | 쉬움 | 빠른 미리보기 |
| APK 배포 | ⚡ 10분 | 무료 | 쉬움 | 내부 테스트 |
| 웹 배포 | ⚡⚡ 5분 | 무료 | 쉬움 | 브라우저 데모 |
| Play Store | 🐌 2-3일 | ₩25,000 | 어려움 | 공식 출시 |

---

## 🔥 상황별 추천

### "지금 당장 화면 보고 싶어요"
→ **FlutLab.io** 또는 **Zapp.run** 사용

### "몇 명에게만 보여주고 싶어요"
→ **APK 빌드** 후 파일 공유

### "인터넷으로 누구나 접속하게 하고 싶어요"
→ **웹 빌드** 후 Netlify/Vercel 배포

### "앱스토어에 정식 출시하고 싶어요"
→ **Google Play Store** 등록

---

## 📋 체크리스트

### APK 배포 전 확인사항
- [ ] 앱 이름/아이콘 설정
- [ ] 권한 설정 (AndroidManifest.xml)
- [ ] 버전 정보 (pubspec.yaml)
- [ ] 난독화 설정 (ProGuard)
- [ ] 앱 서명 (keystore 생성)

### 웹 배포 전 확인사항
- [ ] 반응형 디자인 확인
- [ ] 브라우저 호환성 테스트
- [ ] 이미지 최적화
- [ ] SEO 메타 태그 추가
- [ ] HTTPS 설정

---

## 🛠️ 문제 해결

### "Flutter가 설치되어 있지 않아요"
→ 온라인 IDE 사용하거나 Flutter 설치 필요

### "APK가 설치되지 않아요"
→ "출처를 알 수 없는 앱" 설정 확인

### "앱이 너무 느려요"
→ Release 모드로 빌드했는지 확인 (`--release` 옵션)

### "웹 버전이 모바일에서 이상해요"
→ 반응형 디자인 필요, MediaQuery 사용

---

## 📞 지원

제작: (사)한국시각장애인연합회
- 이메일: kbu1004@hanmail.com
- 전화: 02-799-1000

---

## 🎯 다음 단계

1. ✅ Flutter 설치 또는 온라인 IDE 선택
2. ✅ 앱 실행해서 화면 확인
3. ✅ 필요한 수정사항 반영
4. ✅ 배포 방법 선택
5. ✅ 테스트 사용자에게 공유

**현재 상태**: 기본 UI 완성, 즉시 실행 가능! 🚀
